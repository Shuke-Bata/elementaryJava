1、答：对象（object)代表现实世界中可以明确标识的一个实体。
	类是一个模板、蓝本或者说是合约，用来定义对象的数据域是什么以及方法是做什么的。
	一个对象是类的一个实例。 可以从一个类中创建多个实例。
	就好比印钞票，类就是模子，对象对视印出来的一张张钞票。
2、答：类包括数据域、构造方法、方法，通过关键字class来修饰
3、答：先定义好类，然后以类名声明变量。
4、答：使用new操作符从构造方法创建一个对象
5、答：构造方法必须具备和所在类相同的名字。
	构造方法没有返回值类型，甚至连 void也没有。
	构造方法是在创建一个对象使用new 操作符时调用的。 构造方法的作用是初始化对象。
6、答：当且仅当类中没有明确定义任何构造方法时才会自动提供。
7、答：点操作符(.)
8、答：new声明对象的时候，没有引用变量。直接调用实例方法的方式。这个对象就是匿名对象
9、答：当调用值为 null的引用变量上的方法时会发生此类异常。 在通过引用变量调用一个方法之前，确保先将对象引用賦值给这个变量。
10、答：数组是对象，可以包含对象类型的元素，就是该类型或对象的初始值
11、答：a)、ShowErrors类没有带参数的构造方法，因此无法调用
	b)、类中没有x()实例方法，无法调用
	c)、c一开始声明变量，却没有声明应用变量，所以c的值为null，无法调用方法
	d)、C类没有带参数的构造方法，因此无法调用
12、答：第3行代码创建了一个对象，但是没有无参的构造方法，创建对象失败
13、答：false
14、答：Date date = new Date();	date.getTime();
15、答：Point2D p = new point2D(x,y); p1.distance(p2);
16、答：java.util;javafx.geometry;
17、答：f是实例，所以前四个都正确；F为对象，则只能访问静态变量和方法，所以第六句和第八句正确。第五句和第七句错误。
18、答：第一处和第二处
19、答：静态方法可以调用静态方法、访问静态数据域；但不能调用实例方法，实例方法可以调用静态方法
	代码就是静态方法调用了实例方法。实例方法必须通过创建对象，然后通过对象来调用响应的实例方法
20、答：访问器方法就是访问类中私有数据域的方法，修改器方法就是修改类中私有数据域值的方法。
	访问器get+数据域名，修改器set+数据域名
21、答：便于维护，不易被篡改
22、答：无法获取到radius。实例无法获取到类的私有数据域
23、答：基本类型传递的是值，引用型传递的是对象的引用。
	count is 101
	times is 0
24、答：After swap1: circle1 = 2 circle2 = 1
	After swap2: circle1 = 2 circle2 = 1
25、答：a)、a[0] = 1 a[1] = 2
	b)、a[0] = 2 a[1] = 1
	c)、e1 = 2 e2 = 1
	d)、t1's i = 2 and j = 1
	e)、t2's i = 2 and j = 1
26、答：a)、
	b)、
	c)、
	d)、null
27、答：对象数组初始化的时候，将每个对象引用值都初始化为null。dates[0]，对应的是null，无法toString
28、答：可以改变，这种类不是为不可变对象。还可以通过引用的方式进行修改
29、答：不可以，没有引用类型对象数据类型。
30、答：不可以改变
31、答：i + j is 23
	k is 5
	j is 3
32、答：关键字this引用对象自身。它也可以在构造方法内部调用同一个类的其他构造方法
33、答：10行this.p = p;
34、答：具有两个相同的构造方法